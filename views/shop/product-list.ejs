<%- include('../includes/header.ejs')%>
<link rel="stylesheet" href="/css/product-list.css">
<%- include('../includes/header-body.ejs')%>
<%- include('../includes/navigation.ejs')%>

<main>

    <div class="container">

        <% if(products.length > 0) { %>

        <div class="row">

            <% let i = 0; for(let product of products) { let id = 'product_' + i++  %>

            <div class="col-md-4">
                <div class="thumbnail">
                    <img src="/images/no-product-img.png" alt="" class="img-responsive">
                    <div class="caption">
                        <h4 class="pull-right">$<%= product.price %></h4>
                        <h4><a href="#"><%= product.title %></a></h4>
                        <p><%= product.desc %></p>
                    </div>
                    <div class="ratings">
                        <p>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star checked"></span>
                            <span class="fa fa-star"></span>
                            <span class="fa fa-star"></span>
                            (15 reviews)
                        </p>
                    </div>
                    <div class="space-ten"></div>
                    <div class="btn-ground text-center">
                        <button type="button" class="btn btn-primary"><i class="fa fa-shopping-cart"></i> Add To
                            Cart</button>
                        <a href="/products/<%= product.id %>"><button type="button" class="btn btn-primary"><i
                                    class="fa fa-eye"></i> Details</button></a>
                    </div>
                    <div class="space-ten"></div>
                </div>
            </div>

            <% } %>
        </div>
        <% }  else { %>
        <div class="container">
            <h2>No Products found</h2>
        </div>
        <% } %>
    </div>

</main>

<%- include('../includes/footer.ejs')%>